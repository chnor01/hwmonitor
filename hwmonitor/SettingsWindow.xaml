<Window x:Class="hwmonitor.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hwmonitor"
        mc:Ignorable="d"
        Title="Settings" Height="500" Width="400"
        Background="#121212">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Width" Value="60"/>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#444444" CornerRadius="3"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="RoundedButton">
            <Setter Property="Height" Value="20"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Save" Click="Save_Click" Width="60" Background="#4A90D9" Foreground="White" Style="{StaticResource RoundedButton}" Margin="0,0,8,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80" Background="#2A2A2A" Foreground="White" Style="{StaticResource RoundedButton}"/>
        </StackPanel>

        <ScrollViewer>
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="68"/>
                        <ColumnDefinition Width="68"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="1" Text="Warning" HorizontalAlignment="Center" Foreground="#FFA500"></TextBlock>
                    <TextBlock Grid.Column="2" Text="Critical" HorizontalAlignment="Center" Foreground="#FF4444"></TextBlock>
                </Grid>
                
                <TextBlock Text="CPU" FontSize="14" FontWeight="Bold" Foreground="#4A90D9" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="36"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Temp (°C)"/>
                    <TextBox   Grid.Row="0" Grid.Column="1" Text="{Binding CpuTempWarning}"  Margin="4,0"/>
                    <TextBox   Grid.Row="0" Grid.Column="2" Text="{Binding CpuTempCritical}" Margin="4,0"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Load (%)"/>
                    <TextBox   Grid.Row="1" Grid.Column="1" Text="{Binding CpuLoadWarning}"  Margin="4,0"/>
                    <TextBox   Grid.Row="1" Grid.Column="2" Text="{Binding CpuLoadCritical}" Margin="4,0"/>
                </Grid>

                
                <TextBlock Text="GPU" FontSize="14" FontWeight="Bold" 
                           Foreground="#9B59B6" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="36"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Temp (°C)"/>
                    <TextBox   Grid.Row="0" Grid.Column="1" Text="{Binding GpuTempWarning}"  Margin="4,0"/>
                    <TextBox   Grid.Row="0" Grid.Column="2" Text="{Binding GpuTempCritical}" Margin="4,0"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Load (%)"/>
                    <TextBox   Grid.Row="1" Grid.Column="1" Text="{Binding GpuLoadWarning}"  Margin="4,0"/>
                    <TextBox   Grid.Row="1" Grid.Column="2" Text="{Binding GpuLoadCritical}" Margin="4,0"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Power (W)"/>
                    <TextBox   Grid.Row="2" Grid.Column="1" Text="{Binding GpuPowerWarning}"  Margin="4,0"/>
                    <TextBox   Grid.Row="2" Grid.Column="2" Text="{Binding GpuPowerCritical}" Margin="4,0"/>
                </Grid>

                
                <TextBlock Text="RAM" FontSize="14" FontWeight="Bold" 
                           Foreground="#27AE60" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Usage (%)"/>
                    <TextBox   Grid.Row="0" Grid.Column="1" Text="{Binding RamWarning}"  Margin="4,0"/>
                    <TextBox   Grid.Row="0" Grid.Column="2" Text="{Binding RamCritical}" Margin="4,0"/>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>

</Window>
